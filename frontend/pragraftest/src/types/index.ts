import { BlockToolData } from '@editorjs/editorjs';

/**
 * pragraftest Tool's input and output data format
 */
export interface pragraftestData extends BlockToolData {}

/**
 * pragraftest Tool's configuration object that passed through the initial Editor config
 */
export interface pragraftestConfig {}